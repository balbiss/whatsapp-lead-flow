# Base URL para a API do backend
# No desenvolvimento local com o Vite (npm run dev), o vite.config.ts usará proxy para redirecionar /api para o backend porta 3006.
# Na produção com Nginx / Docker, o Nginx também redirecionará /api para o backend via proxy.
VITE_API_URL=/api
